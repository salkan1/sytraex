# Supabase ücretsiz planda 7 gün hiç istek gelmezse projeyi duraklatır.
# Bu iş haftada iki kez hafif bir okuma isteği göndererek projeyi uyanık tutar.
name: Supabase keepalive
on:
  schedule:
    - cron: '0 6 * * 1,4'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase REST API
        run: |
          curl -fsS "https://lwedkrigshorwkxesjcw.supabase.co/rest/v1/posts?select=id&limit=1" \
            -H "apikey: sb_publishable_9re7jDRcWepctzynNWy2wA_KAIPbDS3" \
            -H "Authorization: Bearer sb_publishable_9re7jDRcWepctzynNWy2wA_KAIPbDS3"
